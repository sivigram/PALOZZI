import { PaletteColour } from '../types/colourAnalysis';import { bestTextColour, formatHsl, formatRgb, pantoneLabel } from '../utils/colourConversions';
export function ColourCard({colour,showTechnical}:{colour:PaletteColour;showTechnical:boolean}){return <article className="colour-card"><div className="swatch" style={{backgroundColor:colour.hex,color:bestTextColour(colour.hex)}}>{colour.category}</div><div className="colour-info"><strong>{colour.name}</strong><span>{colour.hex}</span>{showTechnical&&<><span>{pantoneLabel(colour.pantone,colour.pantoneStatus)}</span><span>{formatRgb(colour.hex)}</span><span>{formatHsl(colour.hex)}</span></>}</div></article>}
