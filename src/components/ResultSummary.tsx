import { SeasonData } from '../types/colourAnalysis';
export function ResultSummary({season}:{season:SeasonData|null}){if(!season)return <section className="card muted"><h2>Final result</h2><p>Select a final subseason to reveal the personalised colour season summary.</p></section>;return <section className="card result" aria-live="polite"><p className="eyebrow">Your colour season is</p><h2>{season.name}</h2><dl className="facts"><div><dt>Season</dt><dd>{season.mainSeason}</dd></div><div><dt>Undertone</dt><dd>{season.undertone}</dd></div><div><dt>Value</dt><dd>{season.value}</dd></div><div><dt>Intensity</dt><dd>{season.intensity}</dd></div><div><dt>Contrast</dt><dd>{season.contrast}</dd></div></dl><p>{season.description}</p><div className="list-grid"><p><b>Best colours:</b> {season.bestColours.join(', ')}</p><p><b>Best neutrals:</b> {season.bestNeutrals.join(', ')}</p><p><b>Recommended metals:</b> {season.metals.join(', ')}</p><p><b>Colours to avoid:</b> {season.avoid.join(', ')}</p></div></section>}
